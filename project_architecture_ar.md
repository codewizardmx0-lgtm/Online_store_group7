# 🏗️ التوثيق المعماري لمشروع (Laravel + Inertia + React)

هذا المستند يقدم تحليلاً عميقاً وشاملاً لهيكلة المشروع لتوضيح كيفية عمل الواجهة الخلفية (Laravel) مع الواجهة الأمامية (React) باستخدام (Inertia.js). تمت كتابة هذا التقرير ليكون مرجعاً للفريق البرمجي.

---

## 1️⃣ شرح عام مبسط (High-Level Explanation)

هذا المشروع هو تطبيق ويب حديث (Modern Web App) يدمج بين قوة **Laravel** في إدارة قواعد البيانات والحماية، وسرعة **React** في بناء واجهات مستخدم تفاعلية، باستخدام **Inertia.js** كجسر تواصل بينهما.

في السابق، كان المشروع عبارة عن تطبيق React منفصل (SPA) يعتمد على `React Router` للتنقل و `Context/LocalStorage` لتخزين البيانات. الآن، تم دمجه داخل Laravel، مما يعني أن **Laravel هو المتحكم الرئيسي** في المسارات (Routes) والبيانات، بينما وظيفة **React** تقتصر على "عرض" الصفحات بدلاً من التحكم الكامل في التطبيق.

**Inertia.js** يسمح لنا ببناء تطبيق بتجربة SPA (بدون إعادة تحميل الصفحة) لكن بدون تعقيدات بناء API منفصل.

---

## 2️⃣ تحليل هيكلة المشروع (Project Structure Analysis)

المشروع يتبع هيكلة Laravel القياسية، مع تركيز الواجهة الأمامية في مجلد `resources/js`.

- `app/` و `routes/`: يمثلان الواجهة الخلفية (Backend). `routes/web.php` يحتوي على جميع مسارات التطبيق.
- `resources/views/app.blade.php`: هو ملف HTML الأساسي (والوحيد) الذي يطلقه Laravel، ويقوم بتحميل ملفات React.
- `resources/js/`: هو قلب الواجهة الأمامية ويحتوي على:
    - `app.jsx`: نقطة البداية (Entry Point) لإعداد Inertia و React.
    - `Pages/`: صفحات التطبيق (كل ملف يمثل صفحة كاملة مثل `Home.jsx`، `Shop.jsx`).
    - `Layouts/`: الهياكل العامة (مثل `CustomerLayout` للعملاء و `AdminLayout` للوحة التحكم).
    - `components/`: مكونات React القابلة لإعادة الاستخدام (أزرار، حقول إدخال، بطاقات منتجات).
    - `context/`: ملفات إدارة الحالة العامة (State Management) باستخدام React Context.
    - `lib/`: ملفات المساعدة والبيانات الوهمية (`mockData.js`، `utils.js`).

---

## 3️⃣ شرح الملفات الأساسية (File-Level Explanation)

### `routes/web.php`

- **ماذا يفعل؟** هو المتحكم الأول بأي طلب يأتي للموقع.
- **كيف يعمل؟** يستقبل الرابط (مثلاً `/shop`) ويقوم بتوجيهه باستخدام `Inertia::render('Shop')`. هذا الأمر يخبر Laravel بجمع البيانات اللازمة (إن وجدت) وإرسالها إلى ملف `Shop.jsx` في React ليتم عرضه.

### `resources/js/app.jsx`

- **ماذا يفعل؟** هو محرك تشغيل الواجهة الأمامية.
- **متى يشتغل؟** يعمل مرة واحدة عند تحميل الموقع في المتصفح.
- **أهميته:**
    1. يبحث عن الصفحة المطلوبة داخل مجلد `Pages`.
    2. يقوم بتطبيق الـ Layout المناسب تلقائياً (مثلاً: يضع `AdminLayout` لأي صفحة يبدأ اسمها بـ `admin/`).
    3. يغلف التطبيق بمزودات الحالة (`Context Providers`) مثل السلة والمفضلة.

### `resources/js/Layouts/CustomerLayout.jsx` و `AdminLayout.jsx`

- **ماذا تفعل؟** هي القوالب الثابتة التي تحتوي على الترويسة (Header) والتذييل (Footer) أو القائمة الجانبية (Sidebar).
- **كيف تتصل؟** يتم تمرير محتوى الصفحة المتغيرة (Children) بداخلها، مما يمنع إعادة تحميل القوائم العلوية عند التنقل بين الصفحات.

### `resources/js/Pages/Shop.jsx` (كمثال لصفحة)

- **ماذا تفعل؟** تمثل صفحة المتجر بالكامل.
- **كيف تعمل؟** تستقبل البيانات وتعرضها. إذا كان هناك فلاتر، تتفاعل مع الرابط لتحديث النتائج، وتستخدم المكونات الفرعية مثل `ProductCard.jsx`.

---

## 4️⃣ تدفق البيانات (Data Flow) ودورة حياة الطلب

هكذا يمر الطلب من متصفح المستخدم حتى تظهر الصفحة:

1.  **المستخدم** يضغط على رابط: `https://yoursite.com/product/1`
2.  **Laravel (web.php)** يستقبل الطلب، ويبحث في المسارات. يجد `Route::get('/product/{id}')`.
3.  **Controller / Route** يقوم بجلب بيانات المنتج من قاعدة البيانات (حالياً تُستخدم بيانات وهمية، لكن هذا هو المسار الطبيعي).
4.  **Inertia** يقوم بتغليف هذه البيانات وإرجاعها على شكل JSON (أو صفحة HTML كاملة إذا كان هذا أول دخول للموقع).
5.  **React (app.jsx)** يتلقى استجابة Inertia، ويبحث عن المكون `Pages/Product.jsx`.
6.  **Product.jsx** يستلم البيانات كـ (Props) (مثل `id` المنتج)، ويقوم برسم (Render) الواجهة.

**أين تُحفظ البيانات (State)؟**

- **بيانات متغيرة ومؤقتة:** مثل حالة السلة والمفضلة وتفاصيل التسجيل تُحفظ حالياً في `React Context` و `LocalStorage` بالمتصفح (إرث من نظام الـ SPA القديم).
- **بيانات دائمة:** بيانات المستخدم الحقيقية تُدار عبر جلسات Laravel (Sessions) وتُمرر لـ React عبر Inertia Shared Props (مثل `usePage().props.auth.user`).

---

## 5️⃣ نظام التوجيه والتنقل (Navigation System)

- **Inertia Routing:** في Inertia، التوجيه يتم في الواجهة الخلفية (Laravel). لا يوجد `react-router-dom` أو `BrowserRouter`.
- **طريقة كتابة الروابط:**
  للتنقل بين الصفحات **بدون** إعادة تحميل الصفحة (SPA feel)، نستخدم مكون `Link` الخاص بـ Inertia:

    ```javascript
    import { Link } from '@inertiajs/react';

    // الطريقة الصحيحة:
    <Link href="/shop">Go to Shop</Link>

    // الطريقة الخاطئة (ستكسر التطبيق):
    <Link to="/shop">Go to Shop</Link> // (تتبع لـ React Router)
    <a href="/shop">Go to Shop</a> // (ستقوم بعمل إعادة تحميل كاملة للصفحة Reload)
    ```

---

## 6️⃣ دليلك للعمل على المشروع (How to Modify)

- **لإضافة صفحة جديدة (مثلاً: About Us):**
    1. أنشئ ملف `About.jsx` في مجلد `resources/js/Pages`.
    2. أضف المسار في `routes/web.php`: `Route::get('/about', fn() => Inertia::render('About'));`.
- **لإضافة حالة عامة (Global State):**
    1. قم بإنشاء Context جديد في مجلد `resources/js/context`.
    2. قم بإضافته داخل `resources/js/app.jsx` ليغلف التطبيق `<App />`.
- **لتعديل القوائم الثابتة:**
    1. اذهب إلى `Layouts/CustomerLayout.jsx` أو `AdminLayout.jsx`.

---

## 7️⃣ المشاكل التي تم اكتشافها وحلها (Problems Detection)

أثناء عملية الدمج (Migration)، كانت هناك بقايا من نظام SPA القديم والتي تسببت في مشاكل، وتم إصلاحها بنجاح:

1.  **بقايا React Router:** كانت بعض الملفات مثل `Shop.jsx` تحاول استدعاء `useSearchParams` و `Navigate` من Inertia (وهي غير موجودة فيه)، مما تسبب في شاشة بيضاء (Crash). **تم الإصلاح** باستخدام الطرق القياسية.
2.  **استخدام خاصية `to=`:** العديد من مكونات `<Link>` كانت تستخدم `to="/path"` بدلاً من `href="/path"`. هذا أدى إلى تعطل التنقل بين الصفحات بالكامل. **تم الإصلاح** في كافة الملفات.
3.  **روابط مفقودة (404):** صفحات مثل السلة والمفضلة ولوحة التحكم كانت موجودة في React لكن لم يكن لها مسارات في `web.php`. **تم الإصلاح**.
4.  **تداخل الصلاحيات (Auth Conflict):** الواجهة كانت تعتمد على LocalStorage لمعرفة هل المستخدم "أدمن" أم لا، بينما Laravel يعتمد على Sessions. **تم الترقيع** بربط `AuthContext` مع بيانات `auth.user` القادمة من Inertia.

---

## 8️⃣ تحسينات معمارية وأفضل الممارسات (Architecture Improvements & Best Practices)

بما أن التطبيق انتقل إلى Laravel + Inertia، يُنصح بشدة باتخاذ الخطوات التالية للتطوير المستقبلي:

### 1. تقليل الاعتماد على React Context (البيانات)

- **الحالة الحالية:** المنتجات تأتي من `mockData.js` عبر `StoreContext`.
- **التحسين:** يجب أن تأتي المنتجات من قاعدة بيانات MySQL عبر متحكمات Laravel (Controllers) وتُمرر كـ `Props` مباشرة إلى الصفحة. يجب الاستغناء عن `StoreContext` لاحقاً.

### 2. نقل السلة والمفضلة إلى الـ Backend

- **الحالة الحالية:** السلة والمفضلة تُحفظ في `LocalStorage`. إذا دخل المستخدم من جهاز آخر سيجد سلة فارغة.
- **التحسين:** إنشاء جداول في قاعدة البيانات للسلة (Carts) والمفضلة، واستخدام `router.post('/cart')` لإضافة المنتجات، وجعل Laravel هو من يعيد حالة السلة في `HandleInertiaRequests.php`.

### 3. المصادقة والحماية (Authentication)

- لا تعتمد على الجافاسكريبت لحماية صفحات الأدمن. يجب التأكد من وجود Middleware في `web.php` يحمي مسارات `admin/` ويسمح فقط لأصحاب رتبة `role === 'admin'` بالدخول.

### 4. بنية المجلدات (Folder Structure)

يُفضل تجميع الصفحات والمكونات المتعلقة بنفس الميزة (Feature-based structure) بدلاً من وضع كل الصفحات في مجلد واحد، مثلاً:
`resources/js/Pages/Shop/Index.jsx`
`resources/js/Pages/Shop/ProductDetail.jsx`

---

**💡 نصيحة أخيرة للفريق:**
تعاملوا مع المشروع على أنه **تطبيق Laravel بامتياز**، و React هو مجرد "طبقة العرض" (View Layer). اجعلوا الـ Backend هو مصدر الحقيقة (Source of Truth) للبيانات والحماية والتوجيه.
