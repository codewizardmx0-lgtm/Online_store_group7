# 📂 هيكلية العمل وتفصيل الملفات (WBS - Work Breakdown Structure)

بناءً على طلبك، هذا تفصيل شامل لكل مجلد وملف داخل `resources/js` (والذي يمثل مجلد `src` في تطبيقات React التقليدية)، بالإضافة إلى الملفات الهامة المرتبطة به في بيئة Laravel + Inertia.

---

## 1️⃣ نقطة البداية (Entry Points)

هذه الملفات هي المحرك الأساسي للواجهة الأمامية بأكملها.

- 📄 **`resources/js/app.jsx`**
    - **الوظيفة:** نقطة الدخول الرئيسية (Entry Point) لتطبيق React.
    - **ماذا يفعل؟** يقوم بإنشاء تطبيق Inertia (`createInertiaApp`)، ويبحث عن الصفحة المطلوبة داخل مجلد `Pages`، ويطبق التخطيط المناسب (Layout) بناءً على اسم الصفحة (مثلاً يطبق `AdminLayout` لصفحات الأدمن)، ويغلف التطبيق بمزودات الحالة (Context Providers).
- 📄 **`resources/js/bootstrap.js`**
    - **الوظيفة:** ملف التهيئة الافتراضي من Laravel.
    - **ماذا يفعل؟** يقوم بإعداد مكتبة `axios` لتكون جاهزة لإرسال الطلبات (AJAX) مع إرفاق توكن الحماية (CSRF Token) تلقائياً، ويمكن استخدامه لإعداد (Laravel Echo) للإشعارات الحية.

---

## 2️⃣ المجلدات الأساسية داخل `resources/js`

### 📁 `Pages/` (الصفحات)

كل ملف هنا يمثل صفحة كاملة يتم استدعاؤها مباشرة من `routes/web.php` عبر `Inertia::render()`.

- 📄 **`Home.jsx` / `Shop.jsx` / `Product.jsx` / `Cart.jsx` ... إلخ:** صفحات العميل (الواجهة الرئيسية للمتجر).
- 📄 **`Login.jsx` / `Register.jsx`:** صفحات تسجيل الدخول المخصصة التي تم تصميمها لتناسب هوية المتجر.
- 📁 **`admin/`**: يحتوي على صفحات لوحة تحكم الإدارة:
    - `Dashboard.jsx` (الإحصائيات)، `Products.jsx` (إدارة المنتجات)، `Orders.jsx` (الطلبات)، وغيرها.
- 📁 **`Auth/`**: صفحات المصادقة الافتراضية التي تأتي مع (Laravel Breeze) (يمكن الاستغناء عنها إن كنت تستخدم `Login.jsx` الخاصة بك).
- 📁 **`Profile/`**: صفحات تعديل الملف الشخصي الخاصة بـ Laravel Breeze.

### 📁 `Layouts/` (الهياكل والتخطيطات)

هنا نضع القوالب الثابتة التي تحيط بالصفحات (Header, Footer, Sidebar) لتجنب إعادة تحميلها.

- 📄 **`CustomerLayout.jsx`:** القالب الخاص بواجهة المتجر (يحتوي على النافبار העلوي وروابط التسوق).
- 📄 **`AdminLayout.jsx`:** القالب الخاص بلوحة تحكم الإدارة (يحتوي على القائمة الجانبية Sidebar).
- 📄 **`GuestLayout.jsx` / `AuthenticatedLayout.jsx`:** قوالب افتراضية من Laravel Breeze (تُستخدم لصفحات الـ Profile والـ Auth).

### 📁 `context/` (إدارة الحالة - State Management)

ملفات تدير البيانات المشتركة بين عدة مكونات (بديلاً عن Redux أو Zustand).

- 📄 **`AuthContext.jsx`:** يدير حالة تسجيل دخول المستخدم وصلاحياته (هل هو Admin أم User؟). تم تعديله مؤخراً ليقرأ البيانات من Inertia.
- 📄 **`StoreContext.jsx`:** يدير بيانات المنتجات والتصنيفات (حالياً يقرأ من `mockData.js`).
- 📄 **`CartContext.jsx`:** يدير سلة المشتريات (إضافة، حذف، تعديل الكمية).
- 📄 **`WishlistContext.jsx`:** يدير قائمة المفضلة.
- 📄 **`ToastContext.jsx`:** يدير الإشعارات المنبثقة (Success/Error Messages).

### 📁 `components/` (المكونات القابلة لإعادة الاستخدام)

قطع الـ UI الصغيرة التي تُستخدم داخل الصفحات للتنظيم.

- 📁 **`ui/` (المكونات المخصصة للمتجر):**
    - `ProductCard.jsx`: بطاقة عرض المنتج.
    - `Button.jsx`, `Input.jsx`, `Modal.jsx`, `Badge.jsx`: مكونات التصميم الأساسية.
- 📄 **المكونات الأخرى (خارج `ui/`):** مكونات افتراضية جاءت مع Laravel Breeze (مثل `TextInput`, `Dropdown`, `PrimaryButton`).

### 📁 `lib/` (المكتبات والمساعدات)

- 📄 **`utils.js`:** يحتوي على دوال مساعدة (Helper Functions) مثل دالة `cn` لدمج كلاسات Tailwind، ودالة `formatCurrency` لتنسيق الأسعار.
- 📄 **`mockData.js`:** قاعدة بيانات وهمية (JSON) تحتوي على المنتجات، التصنيفات، والطلبات بغرض التجربة قبل ربط الـ Backend الفعلي.

### 📁 `assets/` (الملفات الثابتة)

- يحتوي على الصور، الأيقونات، وملفات الـ SVG (مثل `hero.png`).

---

## 3️⃣ ملفات محورية خارج `resources/js` (في الـ Backend)

لأننا في بيئة Laravel، لا يمكن فهم الواجهة الأمامية دون هذه الملفات:

- 📄 **`routes/web.php`** (أهم ملف)
    - هو "شرطي المرور". يستقبل أي رابط يكتبه المستخدم (مثال: `store.com/cart`) ويقوم بتحويله إلى صفحة React المقابلة (`Inertia::render('Cart')`).
- 📄 **`resources/views/app.blade.php`**
    - هو ملف الـ HTML الوحيد في المشروع. مهمته استدعاء حزمة `Vite` لتشغيل وتجميع ملفات React (`@vite(['resources/js/app.jsx'])`).
- 📄 **`app/Http/Middleware/HandleInertiaRequests.php`**
    - هذا الملف هو "الجسر السري" بين لارافل ورياكت. يقوم بتمرير البيانات العامة باستمرار إلى React (Shared Props)، مثل بيانات المستخدم المسجل الدخول `auth.user`، ورسائل الخطأ `errors`.
- 📄 **`vite.config.js`**
    - ملف إعدادات حزمة (Vite) المسؤولة عن تجميع (Bundling) ملفات React وتحويلها لملفات جافاسكريبت عادية يفهمها المتصفح. يقوم أيضاً بإعداد اختصار المسارات (`@/` يشير إلى `resources/js`).
- 📄 **`tailwind.config.js`**
    - إعدادات مكتبة Tailwind CSS، يحدد من أين تقرأ كلاسات التصميم (يقرأ من داخل مجلد `resources/js/**/*.jsx`).
